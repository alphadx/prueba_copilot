services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    volumes:
      - .:/app
      - runtime-data:/app/runtime
    environment:
      - YII_ENV=dev
    stdin_open: true
    tty: true

volumes:
  runtime-data:
